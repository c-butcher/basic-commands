const { assert, expect } = require('chai');
const SubtractCommand = require('../src/subtract-command');

describe('Subtract Command', () => {
    describe('describe()', () => {
        it('passes when it has all the required descriptors', () => {

        });

        /**
         * @NOTE: DON'T EVER CHANGE THE TEST BELOW
         *
         * The command key must always remain the same, otherwise we risk
         * breaking other peoples applications.
         */
        it('passes when the key matches', () => {

        });
    });

    describe('execute()', () => {
        it('passes when an object is returned', () => {

        });

        it('passes when the result contains the added number', () => {

        });
    });
});
